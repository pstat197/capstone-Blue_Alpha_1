export function ReadOnlyNotice() {
  return (
    <div className="readonly-notice">
      Post-run controls are read-only. These pages explore existing generated outputs and do not change YAML or launch runs.
    </div>
  );
}
